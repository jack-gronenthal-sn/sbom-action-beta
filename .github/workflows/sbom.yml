name: servicenow-sbom
run-name: ${{ github.actor }} is uploading a SBOM to ServiceNow.
on:
  workflow_call:
    inputs:
      sbom-repository-name:
        default: jack-gronenthal-sn/sbom-action-beta
        description: The name of the repository holding the workflow.
        required: false
        type: string
      sbom-repository-ref:
        required: true
        type: string
        default: v1.0.22
        description: The tag reference for the SBOM Beta repository which the workflow should check out.
    secrets:
      sn_instance_url:
        required: true
      sn_orchestration_tool_id:
        required: false
      sn_devops_integration_token:
        required: false

jobs:
  checkout-repo:
    runs-on: ubuntu-latest
    steps:
      - name: Sanity check which repo has been checked out
        run: echo "${{ github.repository }}, ${{ github.action_path }}"
      - name: Checkout the repository to the runner
        uses: actions/checkout@v4
#        with:
#          repository: ${{ inputs.sbom-repository-name }}
#          ref: main
      - name: list
        run: ls -A
      - name: list action path
        run: ls -A ${{ github.action_path }}
#  log-inputs:
#    runs-on: ubuntu-latest
#    needs: checkout-repo
#    defaults:
#      run:
#        shell: bash
#        working-directory: ./scripts
#    steps:
#      - name: list
#        run: ls -A >> $GITHUB_OUTPUT
#      - name: Make scripts executable
#        run: chmod +x ./input.sh
#      - name: Log workflow inputs
#        run: ./inputs.sh